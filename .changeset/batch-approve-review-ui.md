---
"@cappa/server": minor
---

Add `POST /api/screenshots/approve-batch` to approve multiple screenshots by name in one request. The review UI uses this for "Approve selected" and "Approve all in category" batch actions.
