---
"@cappa/core": patch
---

Store diff generation metadata in produced PNG files, including the diff algorithm and configured comparison options. When approving screenshots, copy diff metadata onto approved expected PNGs so baseline images retain the accepted diff context.
