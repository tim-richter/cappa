---
"@cappa/core": patch
"@cappa/plugin-storybook": patch
---

Add debug logging when screenshot capture waits for a configured `delay`, and add regression tests to verify delay handling in core screenshots and Storybook-provided screenshot options.
