{
  "name": "cappa",
  "version": "0.0.1",
  "description": "A Playwright-based screenshot tool with plugin system",
  "author": "Tim Richter",
  "license": "MIT",
  "private": true,
  "type": "module",
  "keywords": [
    "screenshot",
    "playwright",
    "capture"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/tim-richter/cappa.git"
  },
  "scripts": {
    "build": "pnpm -r --filter=!@cappa/server build && pnpm -F @cappa/server build",
    "test": "pnpm -r test",
    "dev": "pnpm -r dev",
    "lint": "biome check .",
    "lint:fix": "biome check . --write",
    "tsc": "pnpm -r tsc",
    "attw": "pnpm -r attw"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.12",
    "@changesets/cli": "^3.0.2",
    "@types/node": "catalog:",
    "turbo": "^2.10.12",
    "typescript": "catalog:"
  },
  "packageManager": "pnpm@12.3.4",
  "msw": {
    "workerDirectory": [
      "apps/web/public"
    ]
  }
}
